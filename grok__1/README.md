# Grok 이모님의 $1 찌라시 (Aunt Grok's $1チラシ)\n\n"AI 자본주의 입문기" 대장정의 시작! Grok 이모님이 매일 아침 시장을 훑어 3줄 요약 '찌라시'를 발행합니다. 목표는 딱 1달러! 이 프로젝트는 AI 기반 금융 뉴스레터의 가능성을 탐색하며, 초기 수익 모델을 실험합니다.\n\n## 프로젝트 컨셉\n\n*   **Grok 기반 AI**: (Mock) 시장 데이터를 분석하여 3줄 요약 '찌라시' 생성\n*   **자동 발행**: 이메일 및 (Mock) SNS 채널로 매일 아침 '찌라시' 발행\n*   **$1 목표**: 구독자 수, 광고 클릭 등으로 1달러를 모으는 진행 상황 투명하게 공개\n\n## 주요 기능\n\n1.  **시장 데이터 수집 (Mock)**: 주식/코인 관련 가상 뉴스를 가져옵니다.\n2.  **AI 3줄 요약 엔진 (Mock)**: 수집된 뉴스에서 핵심 내용을 3줄로 요약합니다.\n3.  **찌라시 발행 시스템 (Mock)**: 요약된 내용을 이메일과 SNS 메시지로 가상 발행합니다.\n4.  **$1 프로젝트 진행 상황 추적**: 현재 수익, 구독자 수, 광고 클릭 수를 추적하고 표시합니다.\n\n## 실행 방법\n\n### 1. 가상 환경 설정\n\n파이썬 3.7+ 버전이 설치되어 있어야 합니다.\n\n```bash\n# 가상 환경 생성\npython -m venv venv\n\n# 가상 환경 활성화\n# Windows\n.\\venv\\Scripts\\activate\n# macOS/Linux\nsource venv/bin/activate\n\n# 필요한 라이브러리 설치 (이 프로젝트는 기본 라이브러리만 사용합니다)\n# pip install requests # 만약 실제 API를 사용한다면 필요할 수 있습니다.\n```\n\n### 2. 환경 변수 설정 (Mocking을 위한 가상 설정)\n\n이 프로젝트는 실제 API 호출 대신 Mocking 로직을 사용하지만, 실제 프로젝트를 위한 환경 변수 사용 방식을 보여주기 위해 `.env` 파일을 통해 설정할 수 있습니다.\n\n`grok_auntie_bot.py` 파일은 `os.getenv`를 사용하여 환경 변수를 읽습니다. `.env` 파일을 생성하고 다음 내용을 추가하세요:\n\n```env\nMOCK_GROK_API_KEY="your_mock_grok_api_key_here"\nMOCK_EMAIL_SENDER="your_mock_email@example.com"\nMOCK_EMAIL_RECIPIENTS="sub1@example.com,sub2@example.com"\nMOCK_SNS_CHANNEL_ID="mock_telegram_channel_id"\n```\n\n*   `MOCK_GROK_API_KEY`: Grok AI API 키를 대신할 가상 키입니다. 실제 Grok API를 사용하게 될 경우 여기에 실제 키를 넣게 됩니다.\n*   `MOCK_EMAIL_SENDER`: 발신자 이메일 주소입니다.\n*   `MOCK_EMAIL_RECIPIENTS`: 수신자 이메일 주소 목록입니다 (쉼표로 구분).\n*   `MOCK_SNS_CHANNEL_ID`: 가상으로 메시지를 보낼 SNS 채널 ID입니다.\n\n**참고**: 이 프로젝트는 `python-dotenv` 라이브러리를 사용하지 않으므로, 환경 변수는 시스템 환경 변수로 설정하거나 `os.environ['KEY'] = 'VALUE'` 형태로 코드 내에서 직접 설정해야 합니다. `os.getenv`는 설정되지 않은 경우 기본값을 사용합니다.\n\n### 3. 봇 실행\n\n가상 환경이 활성화된 상태에서 다음 명령어를 실행합니다:\n\n```bash\npython grok_auntie_bot.py\n```\n\n봇이 실행되면, 가상 시장 데이터를 가져와 Grok AI가 3줄 요약을 생성하고, 이메일 및 SNS로 발행을 시뮬레이션한 후, $1 목표 달성 진행 상황을 콘솔에 표시합니다. `grok_auntie_data.json` 파일에 진행 상황이 저장됩니다.\n\n### 4. 반복 실행 및 진행 상황 확인\n\n봇을 여러 번 실행하면 `grok_auntie_data.json`에 저장된 상태를 기반으로 구독자 수와 수익이 점진적으로 증가하는 것을 볼 수 있습니다. 봇은 실행될 때마다 `$1` 목표를 향한 진행 상황을 업데이트합니다.\n\n## 프로젝트 구조\n\n```\n.\n├── grok_auntie_bot.py    # 메인 봇 로직\n├── grok_auntie_data.json # 프로젝트 진행 상황 저장 (자동 생성)\n└── README.md             # 프로젝트 설명서 (현재 파일)\n```\n\n## 미래 확장 아이디어 (Placeholder)\n\n*   **실제 Grok API 연동**: xAI의 Grok API (또는 다른 LLM API)를 사용하여 실제 시장 분석 수행.\n*   **다양한 시장 데이터 소스**: 뉴스 스크래핑, 증권사 API, 코인 거래소 API 등 실제 데이터 연동.\n*   **실제 이메일/SNS 발행**: `smtplib`를 이용한 이메일 발송, Telegram/Slack/Discord 봇 연동.\n*   **수익 모델 확장**: 실제 광고 플랫폼 연동, 프리미엄 구독 모델 등.\n*   **자동 스케줄링**: `cron` 또는 클라우드 함수(AWS Lambda, Google Cloud Functions)를 이용한 매일 아침 자동 실행.\n\n이 프로젝트는 AI 기반 금융 뉴스레터의 첫걸음이자, 1달러를 향한 작은 모험입니다.\n